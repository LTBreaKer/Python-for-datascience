def count_int_list(l1: list[str], word: str) -> int:
    """
        def count_int_list(l1: list[str], word: str) -> int

        Takes two arguments:
            - l1 : list[str]
            - word: str

        returns the number of `word` in `l1`
    """
    return l1.count(word)
